const { Router } = require("express");
const router = Router();
const cart = []
router.get("/", (req, res) => {
    console.log('carrito')
});
router.post
router.put
router.delete
module.exports = router;
